{
  "name": "third-place-finder",
  "version": "1.0.0",
  "description": "Agentic AI Spot Scout and Cafe Mixer",
  "scripts": {
    "postinstall": "npm install --prefix backend && npm install --prefix frontend",
    "start": "concurrently \"node backend/server.js\" \"npm run dev --prefix frontend -- --force\"",
    "up": "npm install && npm start"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
